# Public demo script — target 2:35

Language: English. Output: 1920×1080, public YouTube, burned-in English captions plus `.srt`. Every screen must be captured from the public DungeonQ deployment or generated from repository-owned typography and shapes.

| Time | Picture | Narration |
|---:|---|---|
| 0:00–0:14 | DungeonQ hero and claim boundary | “Security agents can propose powerful defenses. But should they approve their own actions? DungeonQ is a synthetic cyber-defense proving ground built around one answer: no.” |
| 0:14–0:31 | WebMCP agent path graphic | “With WebMCP, an agent can bring a complete judge-authored dungeon into the page. The pack is data only: synthetic IDs, bounded size, no URLs, credentials, scripts, or real targets.” |
| 0:31–0:51 | Agent discovers tools and calls Scenario admission | “The agent discovers a tool shaped for Scenario admission. The same authoritative validator serves WebMCP, the visual editor, and the CLI. A valid pack creates a fresh, browser-local session.” |
| 0:51–1:10 | Simulation result: risk, route, digest, capabilities | “Now the deterministic engine weighs source trust, composes failures, chooses a route overlay, and binds the proposal to a canonical digest. The model is not a security authority.” |
| 1:10–1:32 | Agent requests review; tools change; human control highlighted | “The agent requests review—and then stops. There is no approval tool. Only this visible human control can approve the exact proposal digest and revision.” |
| 1:32–1:52 | Human approves; agent applies and verifies | “After human approval, WebMCP exposes a new, narrower capability. The agent applies a synthetic local transition and verifies the protected receipt against read-back.” |
| 1:52–2:10 | Compensation and audit chain | “Recovery does not erase history. Compensation appends a linked receipt while the original effect and audit chain remain available for replay.” |
| 2:10–2:25 | Compound failure scenario and blocked capability view | “Injected failures can only remove capabilities. Missing edge context fails closed, and all one hundred twenty-seven failure combinations are checked for monotonic safety.” |
| 2:25–2:35 | Public source, live URL, final hero | “Bring your own synthetic dungeon, inspect the proof, and challenge the boundary. DungeonQ: enter the unknown, exit with proof.” |

## Required live evidence in the recording

- Public URL visible at least once.
- WebMCP tool discovery or call result visible.
- `dungeonq_scenario_admit` succeeds with a non-default synthetic Scenario ID.
- Agent stops at `HUMAN_UI`; human approval is a separate visible click.
- Apply receipt, verification, compensation, and retained audit entries are visible.
- Synthetic boundary appears in opening or closing frame.

## Claims that must not appear

- “Production ready,” “unhackable,” “real-time threat prevention,” or field detection rates.
- Real company, system, credential, log, employee, customer, or infrastructure data.
- Windows Agent, ERP, or any private parent-project material.
